// JavaScript Document// asks before deleting item.function confirmar(url,msg) {	doyou = confirm(msg)	if(doyou == true) document.location.href = url;}// goto to add new imagefunction addImage(album_id){	document.location.href = "add_image.php?id=" + album_id;}// go to see imagesfunction listImages(album_id){	document.location.href = "list_images.php?id=" + album_id;}function getParamValue(param,url){	var u = url == undefined ? document.location.href : url;	var reg = new RegExp('(\\?|&|^)'+param+'=(.*?)(&|$)');	matches = u.match(reg);	return matches[2] != undefined ? decodeURIComponent(matches[2]).replace(/\+/g,' ') : '';}